import { AdminDashboard } from '../dashboard/AdminDashboard'

/**
 * Admin page.
 * Reuses the shared admin dashboard UI used by the role-based dashboard route.
 */
export default function AdminPage() {
  return <AdminDashboard />
}
